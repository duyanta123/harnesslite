# Compose the desktop-owned client seam around the upstream Web profile.
# Browse remains the universal directory flow; the injected HarnessLite client
# adds a native shortcut only when the page is inside the local Tauri shell.
- id: directory-picker
  name: '@deepseek-ai/dsh-host-directory-picker-auto'
  disabled: true
- insert:
    - id: harnesslite-directory-picker-browse-host
      name: '@deepseek-ai/dsh-host-directory-picker-browse'
    - id: harnesslite-directory-picker-browse-surface
      name: '@deepseek-ai/dsh-client-ui-directory-picker-browse'
    - id: harnesslite-client-integration
      name: '@duyanta123/harnesslite-integration'
